# Sparse Retrieval Comparison

Benchmarks six sparse retrieval encoders (BM25, TF-IDF, SPLADE,
OpenSearch EN/multilingual, BGE-M3) on English/Korean OEM manual text.

## Run

```bash
pip install -e .
python generate_results.py
```
